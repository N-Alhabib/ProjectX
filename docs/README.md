# Docs

## PRD
The PRD lives at `docs/PRD.html`.

To update it, open `docs/PRD.html` in your editor and edit the HTML directly.

## Setup
1. Copy `.env.example` to `.env.local`.
2. Fill in `DATABASE_URL` and `SESSION_SECRET`.
3. Run `npm install`.
4. Run `npm run dev`.
5. Visit `/health`.
